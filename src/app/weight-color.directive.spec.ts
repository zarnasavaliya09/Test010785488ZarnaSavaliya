import { WeightColorDirective } from './weight-color.directive';

describe('WeightColorDirective', () => {
  it('should create an instance', () => {
    const directive = new WeightColorDirective();
    expect(directive).toBeTruthy();
  });
});
